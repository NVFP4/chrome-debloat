#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Action {
    ActivateDialogButton,
    BackspacePolicyEdit,
    BackspaceFilter,
    BeginPolicyEdit,
    BeginFilter,
    CancelPolicyEdit,
    CancelFilter,
    CloseDialog,
    CommitPolicyEdit,
    CommitFilter,
    ConfirmApply,
    ConfirmQuit,
    ConfirmRevert,
    ConfirmUninstall,
    InputFilter(char),
    InputPolicyEdit(char),
    MovePolicyGroup(i16),
    MovePolicyCursor(i16),
    MovePolicyType(i16),
    MoveDialogFocus(i16),
    NewPolicyItem,
    Noop,
    OpenApplyDialog,
    OpenReportIssue,
    OpenRevertDialog,
    OpenUninstallDialog,
    Paste(String),
    PolicyCursorEnd,
    PolicyCursorStart,
    Quit,
    Redraw,
    Redo,
    ScrollHelp(i16, u16),
    SelectTab(usize),
    StagePolicyRemoval,
    Tick,
    ToggleHelp,
    TogglePolicyPresence,
    Undo,
}
